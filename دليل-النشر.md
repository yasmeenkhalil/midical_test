# دليل النشر — OMSPrep + Atlas (مع نظام الاشتراك)

هذا الدليل يشرح كيف تنشر التطبيق على **Cloudflare** (مجاني للبداية) مع نظام دفع واشتراك حقيقي.
كل الملفات في هذا المجلد. اتبع الخطوات بالترتيب.

> **مبدأ الحماية:** المحتوى المدفوع (الـ ٦٠٪: المستوى المتوسط والعميق في التشريح، وآخر ٦٠٪ من كل موضوع سريري)
> **لا يوجد إطلاقاً داخل ملف `index.html`**. هو محفوظ على خادم Cloudflare ولا يُرسَل إلا لمشترك فعّال
> أو لمن فعّل المعاينة المجانية (ساعتان). هكذا لا يستطيع أحد سرقته من شيفرة الصفحة.

---

## ملفات هذا المجلد

| الملف | الوصف | أين يذهب |
|------|-------|----------|
| `index.html` | التطبيق (يحوي المحتوى المجاني فقط = ٤٠٪) | Cloudflare **Pages** |
| `manifest.json`, `sw.js`, `icon.svg` | ملفات الـ PWA (تطبيق قابل للتثبيت + عمل دون إنترنت) | Cloudflare **Pages** (بجانب index) |
| `admin.html` | لوحتك الخاصة لتفعيل دافعي ZainCash يدوياً | احتفظ بها لك (أو Pages) |
| `worker.js` | الخادم الخلفي (تسجيل/دخول/دفع/تفعيل/تقديم المحتوى المدفوع) | Cloudflare **Worker** |
| `wrangler.toml` | إعدادات الـ Worker | مع worker.js |
| `schema.sql` | جداول قاعدة البيانات | يُنفَّذ على D1 |
| `paid-content.json` | المحتوى المدفوع (٦٠٪) | يُرفع إلى **KV** |

---

## قبل أن تبدأ: الحسابات التي يجب أن تنشئها بنفسك

> لا أستطيع إنشاء حسابات نيابةً عنك. هذه الخطوات يجب أن تقوم بها أنت:

1. **حساب Cloudflare** مجاني: <https://dash.cloudflare.com/sign-up>
2. **حساب تاجر PayTabs** (للدفع بالبطاقة تلقائياً): <https://www.paytabs.com> — تسجيل كتاجر، ستحصل على
   `Profile ID` و `Server Key`. (PayTabs يغطّي العراق والأردن والكويت وعُمان والسعودية والإمارات ومصر.)
3. **حساب تاجر ZainCash** (للتحويل اليدوي): <https://zaincash.iq> — للتفعيل اليدوي للطلاب الذين يحوّلون عبر زين كاش.

> ملاحظة: يمكنك البدء والاختبار **بدون** PayTabs/ZainCash — التسجيل والدخول والمعاينة المجانية تعمل فوراً.
> فعّل بوابات الدفع لاحقاً عندما تكون جاهزاً.

---

## الجزء الأول: تثبيت الأدوات

على جهازك (يحتاج **Node.js**: <https://nodejs.org>):

```bash
npm install -g wrangler        # أداة Cloudflare لسطر الأوامر
wrangler login                 # يفتح المتصفح لتسجيل الدخول لحسابك
```

---

## الجزء الثاني: قاعدة البيانات (D1) ومخازن المفاتيح (KV)

من داخل المجلد الذي فيه `wrangler.toml`:

### 1) أنشئ قاعدة بيانات D1
```bash
wrangler d1 create omsprep-db
```
سيعطيك سطراً فيه `database_id = "..."`. **انسخ هذا الرقم.**

### 2) أنشئ مخزَنَي KV
```bash
wrangler kv namespace create SESSIONS
wrangler kv namespace create PAID
```
كل أمر يعطيك `id = "..."`. **انسخ الرقمين.**

### 3) ضع الأرقام الثلاثة في `wrangler.toml`
افتح `wrangler.toml` بمحرّر نصوص واستبدل:
- `PUT_YOUR_D1_DATABASE_ID_HERE` ← رقم قاعدة D1
- `PUT_YOUR_SESSIONS_KV_ID_HERE` ← رقم SESSIONS
- `PUT_YOUR_PAID_KV_ID_HERE` ← رقم PAID

### 4) أنشئ الجداول داخل قاعدة البيانات
```bash
wrangler d1 execute omsprep-db --remote --file=./schema.sql
```

### 5) ارفع المحتوى المدفوع إلى KV
```bash
wrangler kv key put --binding=PAID "PAID_CONTENT" --path=./paid-content.json --remote
```
> هذا هو الـ ٦٠٪ المدفوع. لاحظ مفتاح التخزين اسمه بالضبط **`PAID_CONTENT`**.

---

## الجزء الثالث: الأسرار (كلمات السر للخادم)

الأسرار **لا** تُكتب في `wrangler.toml`. اضبطها بالأوامر التالية (كل أمر سيطلب منك القيمة):

```bash
wrangler secret put ADMIN_TOKEN          # اخترع نصاً طويلاً عشوائياً واحفظه (لتفعيل ZainCash اليدوي)
# عند تفعيل PayTabs لاحقاً:
wrangler secret put PAYTABS_PROFILE_ID
wrangler secret put PAYTABS_SERVER_KEY
```

ثم في `wrangler.toml` تحت `[vars]` عدّل:
- `SITE_URL` ← سيصبح رابط موقعك بعد النشر (مثلاً `https://omsprep.pages.dev`) — يمكنك تركه الآن وتعديله بعد الجزء الخامس.
- `PAYTABS_REGION` ← منطقة حساب PayTabs (`ARE` للإمارات، `SAU` للسعودية، `JOR` للأردن، `EGY` لمصر… أو `GLOBAL`).
- `PAYTABS_CURRENCY` ← عملة حساب PayTabs (مضبوطة على `IQD`؛ لو حسابك بعملة أخرى ضع رمزها).

---

## الجزء الرابع: نشر الخادم (Worker)

```bash
wrangler deploy
```
ستحصل على رابط مثل:
```
https://omsprep-api.YOUR-SUBDOMAIN.workers.dev
```
**انسخ هذا الرابط.** هذا هو عنوان الـ API.

---

## الجزء الخامس: ربط التطبيق بالخادم ونشره

### 1) ضع رابط الـ API في `index.html`
افتح `index.html` وابحث عن:
```js
window.OMS_API_BASE || "https://omsprep-api.YOUR-SUBDOMAIN.workers.dev"
```
استبدل الرابط برابط الـ Worker الذي حصلت عليه. (يمكنك بدلاً من ذلك إضافة سطر قبله:
`window.OMS_API_BASE = "https://..."؛`)

### 2) انشر الواجهة على Cloudflare Pages
أسهل طريقة من لوحة Cloudflare:
1. **Workers & Pages → Create → Pages → Upload assets**.
2. ارفع ملفات الواجهة فقط: `index.html`, `manifest.json`, `sw.js`, `icon.svg` (لا ترفع worker.js ولا paid-content.json هنا).
3. ستحصل على رابط مثل `https://omsprep.pages.dev`.

أو بسطر الأوامر:
```bash
wrangler pages deploy . --project-name=omsprep
```

### 3) حدّث `SITE_URL` وأعد نشر الـ Worker
ضع رابط Pages في `wrangler.toml` تحت `SITE_URL`، ثم:
```bash
wrangler deploy
```

> **مهم (CORS):** الـ Worker يسمح بالطلبات من أي مصدر افتراضياً. هذا يكفي للبداية.
> لاحقاً يمكن حصره على رابط موقعك فقط لمزيد من الأمان.

---

## الجزء السادس: ربط PayTabs (الدفع التلقائي بالبطاقة)

في لوحة تاجر PayTabs:
1. فعّل **Hosted Payment Page**.
2. اضبط **Webhook / IPN URL** على:
   ```
   https://omsprep-api.YOUR-SUBDOMAIN.workers.dev/api/pay/webhook
   ```
3. اضبط صفحة الرجوع (Return URL) على رابط موقعك (مثلاً `https://omsprep.pages.dev`).

عند الدفع الناجح: PayTabs يُعلِم الخادم → الخادم **يتحقق من الدفع مباشرةً** ثم يفعّل الاشتراك تلقائياً.
الطالب يعود للموقع فيجد المحتوى مفتوحاً.

> الأسعار المضبوطة: **شهرين = 5000 دينار، ثلاثة أشهر = 10000، سنة = 25000**. لتعديلها غيّر `PLANS` في أعلى `worker.js` ثم `wrangler deploy`.

---

## الجزء السابع: تفعيل دافعي ZainCash يدوياً

ZainCash هنا **تحويل يدوي**: الطالب يحوّل لك، ثم تفعّل أنت حسابه.

افتح `admin.html` في متصفحك (ملف خاص بك)، واملأ:
- **رابط الـ API**: رابط الـ Worker.
- **الرمز السرّي**: قيمة `ADMIN_TOKEN` التي ضبطتها.
- **بريد الطالب**: نفس البريد الذي سجّل به في التطبيق.
- **الخطة**: شهرين / ثلاثة أشهر / سنة.

اضغط «تفعيل الاشتراك» — يُفتح المحتوى للطالب فوراً عند تحديث صفحته.

---

## كيف يعمل النظام للطالب

1. يفتح الموقع → يرى **٤٠٪ مجاناً** (المستوى السطحي في التشريح، وأول ٤٠٪ من كل موضوع سريري).
   باقي المحتوى **لا يظهر إطلاقاً** (لا قفل ولا تمويه — يختفي تماماً).
2. يسجّل حساباً (بريد + كلمة سر).
3. يمكنه تجربة **معاينة مجانية كاملة لمدة ساعتين** (مرة واحدة لكل حساب).
4. للاشتراك: إمّا بطاقة عبر PayTabs (فتح تلقائي)، أو تحويل ZainCash (تفعّله أنت).
5. بعد الاشتراك → يظهر المحتوى الكامل، ويُختم باسم بريده (علامة مائية) لردع التسريب.

---

## ملاحظات مهمة

- **الأمان الحقيقي:** المحتوى المدفوع على الخادم فقط؛ لا يمكن استخراجه من الصفحة. هذا أقوى بكثير من إخفاء بصري في المتصفح.
- **التكلفة:** خطة Cloudflare المجانية تكفي لبداية قوية (آلاف الطلبات يومياً). تدفع فقط عند نمو كبير.
- **رسوم PayTabs:** تقتطع نسبة من كل عملية (حوالي ٢٫٢٥٪) — راجع عقدك معهم.
- **محتوى علمي:** المادة مُولّدة من مراجع قياسية (Snell وكتب جراحة الفم والوجه والفكين). راجِعها بنفسك قبل الاعتماد السريري والامتحاني — فأنت المرجع الأعلى.
- **تحديث المحتوى لاحقاً:** عدّل المحتوى المدفوع، ثم أعد رفعه:
  `wrangler kv key put --binding=PAID "PAID_CONTENT" --path=./paid-content.json --remote`
  وعدّل المجاني داخل `index.html` ثم أعد نشر Pages.

---

## ملخص الأوامر (نسخة سريعة)

```bash
npm install -g wrangler
wrangler login

wrangler d1 create omsprep-db
wrangler kv namespace create SESSIONS
wrangler kv namespace create PAID
# ← ضع الأرقام الثلاثة في wrangler.toml

wrangler d1 execute omsprep-db --remote --file=./schema.sql
wrangler kv key put --binding=PAID "PAID_CONTENT" --path=./paid-content.json --remote

wrangler secret put ADMIN_TOKEN
# (لاحقاً) wrangler secret put PAYTABS_PROFILE_ID ; wrangler secret put PAYTABS_SERVER_KEY

wrangler deploy                      # ينشر الخادم، يعطيك رابط الـ API
# ← ضع رابط الـ API في index.html

wrangler pages deploy . --project-name=omsprep   # ينشر الواجهة
# ← ضع رابط Pages في SITE_URL داخل wrangler.toml
wrangler deploy                      # إعادة نشر الخادم بعد ضبط SITE_URL
```


<!-- الكتب الموجودة  -->
"Contemporary Oraland Maxillofacial Surgery 5th Ed_260529_203157"
"An Outline of Oral Surgery Part 1 - Part 1"
